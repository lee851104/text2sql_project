# ── 課程與參考教材（不屬於本專案產出，且含未去識別化的真實電號）──
# 智能電力分析系統/ 的文件內有真實電號殘留（見 03-元件規格.md、04-API契約.md），
# 該教材自己的「教材化注意事項」也要求公開前先改成假名。
# 要納入版控前請先去識別化。
架構參考/

# ── 資料產物（依規格 §9：raw / interim / processed 都不進版控）──
# 注意：corpus/ 與 benchmarks/ 必須進版控，不可放到 data/ 底下
data/

# 原始快照與 release 壓縮包由 GitHub Releases 保存，不重複提交到 Git
/files/
/manifest.csv
/manifest.json
/releases/**/*.zip

# 合併門檻檢查報告：本機每次執行都會產生一份，屬於過程紀錄不是專案產出
/reports/merge_check/

# 暫存實驗目錄，不屬於專案產出
/extensions/

# ── 機密 ──
.env
*.key
.powerquery-public/
logs/

# ── Python ──
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
*.egg-info/

# ── 作業系統 / 編輯器 ──
.DS_Store
Thumbs.db
.vscode/
.idea/

# Codex 建置暫存目錄（含 node_modules），不屬於專案產出
/.codex-*/
